version: '3'
services: 
    db:
        image: 'postgres:latest'
        container_name: postgres_db
        environment:
            POSTGRES_PASSWORD: postgres
            POSTGRES_USER: postgres
            POSTGRES_DB: pebmedapi
        ports:
            - "5433:5432"